language: node_js

node_js:
- node

branches:
  only:
  - master

before_script:
- npm install -g --silent

script:
- gulp build --prod

after_script:
- gulp deploy --user $FTP_USER --password $FTP_PASSWORD